# Examples

`azure-functions-langgraph` keeps a growing set of smoke-tested examples:

| Role | Path | Description |
| --- | --- | --- |
| Minimal | `examples/simple_agent` | Two-node greeting agent with sequential edges. |

Each example is a standalone Azure Functions app. See the README in each directory for local setup instructions.
